import 'package:flutter/material.dart';

class ReviewFlashcardPage extends StatelessWidget {
  const ReviewFlashcardPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
